# Graph runtime
langgraph>=0.2
langchain-core>=0.3

# LLM client (DeepSeek uses OpenAI-compatible API)
openai>=1.40
httpx>=0.27

# Web server
fastapi>=0.110
uvicorn[standard]>=0.30

# Data / typing
pydantic>=2.5
typing-extensions>=4.10
numpy>=1.26

# Retrieval (FAISS is optional; search_catalog falls back to BM25 + hash embedding when missing)
faiss-cpu>=1.7
sentence-transformers>=2.7
